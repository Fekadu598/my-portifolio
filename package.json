{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Fekadu's Portfolio — Backend, Frontend, and 3 Admin Dashboards",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin && npm install --prefix ui-forge && npm install --prefix server-ops",
    "seed": "npm run seed --prefix backend",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "admin": "npm start --prefix admin",
    "ui-forge": "npm start --prefix ui-forge",
    "server-ops": "npm start --prefix server-ops",
    "dev": "npx concurrently -k \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
